# AXIS // archive

This directory holds old, non-active artifacts that are kept only for historical reference.

Do **not** wire anything in here into the current build.

- `standalone_preview.html` — a 3,000+ line self-contained preview from the early prototype era. It uses an old purple palette and predates the current capsule / modal / V4 work. It is **not** the active build path. `index.html` + `styles.css` are the source of truth.
