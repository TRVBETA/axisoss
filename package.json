{
  "name": "axisoss",
  "version": "5.1.0",
  "private": true,
  "description": "AXIS V5.1 — personal operating system dashboard with The Wanderer",
  "scripts": {
    "test": "for f in test_*.mjs; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "playwright": "^1.62.0",
    "ws": "^8.21.1"
  }
}
